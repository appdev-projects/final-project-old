<div class="jumbotron jumbotron-fluid">
  <div class="container">
    <h1 class="display-4">Where you should go</h1>
  </div>
</div>

<div class="container">
    <table class="table">
      <tr>
        <th>
          City
        </th>
        
        <th>
          Score
        </th>
      </tr>
        
        <% tempHash =  @cityRanking.sort_by(&:last).reverse
        tempHash.each do | city |  %>
        <tr>
            <td>
                <a href="/locations/<%= city[0] %>">
                    <%= Location.find(city[0]).city_name %>
                </a>
                
            </td>
            <td>
                <%= city[1].to_s %>
            </td>

         </tr>
        <% end %>
    </table>
</div>
